import { useState } from "react";
const { View, Text, StyleSheet } = require("react-native");

const TopMenu = () => {
  return (
    <View>
      <Text> TopMenu component</Text>
    </View>
  );
};

export default TopMenu;
